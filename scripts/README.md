# Скрипты автоматической установки

Эта директория содержит скрипты для автоматизации установки и настройки среды AI-разработки на Windows.

## Скрипты

### 🚀 windows-setup.ps1

**Автоматическая установка основных инструментов для AI-разработки на Windows**

Устанавливает:
- **Desktop-приложения:** Claude Desktop, ChatGPT Desktop
- **Базовые инструменты:** Node.js LTS, Git
- **Редактор:** Visual Studio Code
- **Расширения VS Code:** Claude, Codex, GitLens, Error Lens, Prettier, ESLint
- **CLI-инструменты:** GitHub CLI, Firebase CLI
- **MCP серверы:** filesystem, github
- **Конфигурация:** Создает базовую настройку для Claude Desktop

> ⚠️ **Примечание:** Claude Code CLI и Codex CLI не включены в этот скрипт. Для их установки используйте `install-cli-tools.ps1`.

#### Использование

1. **Откройте PowerShell от имени администратора**:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (Администратор)" или "Терминал (Администратор)"

2. **Разрешите выполнение скриптов** (если еще не разрешено):
   ```powershell
   Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

3. **Перейдите в директорию со скриптом**:
   ```powershell
   cd "d:\PROJECTS\IlyaAI\ai-dev-instructions\scripts"
   ```

4. **Запустите скрипт**:
   ```powershell
   .\windows-setup.ps1
   ```

5. **Следуйте инструкциям на экране**

#### Что делает скрипт

- ✅ Проверяет наличие каждого инструмента перед установкой
- ✅ Использует `winget` для установки основных программ
- ✅ Использует `npm` для установки CLI-инструментов
- ✅ Настраивает базовую конфигурацию Git
- ✅ Создает конфигурацию MCP серверов для Claude Code
- ✅ Показывает финальную статистику установки
- ✅ Предоставляет следующие шаги для завершения настройки

---

### 🔧 install-cli-tools.ps1

**Установка Claude Code CLI и Codex CLI**

> ⚠️ **ВАЖНО:** Эти инструменты требуют API-доступа и оплаты отдельно от подписки!

Устанавливает:
- Claude Code CLI (требует Anthropic API key)
- Codex CLI (требует OpenAI API key)
- Настраивает переменные окружения для API-ключей

#### Использование

1. **Прочитайте инструкцию** [docs/06-cli-tools-api.md](../docs/06-cli-tools-api.md)

2. **Получите API-ключи:**
   - Anthropic: https://platform.anthropic.com/settings/keys
   - OpenAI: https://platform.openai.com/api-keys

3. **Запустите скрипт:**
   ```powershell
   .\install-cli-tools.ps1
   ```

4. **Введите API-ключи** когда скрипт запросит

---

### 🗑️ uninstall.ps1

**Деинсталляция среды AI-разработки**

Удаляет инструменты, установленные через `windows-setup.ps1` и `install-cli-tools.ps1`.

#### Использование

**Интерактивный режим** (с запросами по каждому инструменту):
```powershell
.\uninstall.ps1
```

**Автоматический режим** (без запросов):
```powershell
.\uninstall.ps1 -Force
```

**Выборочное удаление:**
```powershell
# Не удалять Desktop-приложения
.\uninstall.ps1 -SkipDesktopApps

# Не удалять CLI-инструменты
.\uninstall.ps1 -SkipCLI

# Не удалять VS Code
.\uninstall.ps1 -SkipVSCode

# Не удалять конфигурационные файлы
.\uninstall.ps1 -SkipConfig

# Комбинация параметров
.\uninstall.ps1 -Force -SkipDesktopApps -SkipConfig
```

#### Что удаляет

- Desktop-приложения (Claude Desktop, ChatGPT Desktop)
- CLI-инструменты (Claude Code CLI, Codex CLI, Firebase CLI, GitHub CLI)
- MCP серверы
- Расширения VS Code
- VS Code (опционально)
- Node.js и Git (опционально)
- Конфигурационные файлы
- API-ключи из переменных окружения

---

### 🔍 check-installation.ps1

**Проверка корректности установки инструментов**

Проверяет:
- Наличие базовых инструментов (Node.js, Git, VS Code)
- Наличие внешних CLI (GitHub, Firebase)
- Конфигурацию Claude Desktop (MCP)
- Авторизацию в сервисах
- Установленные расширения VS Code

#### Использование

1. **Откройте PowerShell** (права администратора не требуются)

2. **Перейдите в директорию со скриптом**:
   ```powershell
   cd "d:\PROJECTS\IlyaAI\ai-dev-instructions\scripts"
   ```

3. **Запустите скрипт**:
   ```powershell
   .\check-installation.ps1
   ```

#### Что показывает скрипт

- ✓ Зеленым — установлено и работает
- ✗ Красным — не установлено или не настроено
- ⚠ Желтым — частичная настройка или предупреждения
- Итоговую статистику установки (процент готовности)
- Список следующих шагов для завершения настройки

---

## Требования

### winget (Windows Package Manager)

Скрипт `windows-setup.ps1` использует `winget` для автоматической установки программ.

**Проверьте наличие winget:**
```powershell
winget --version
```

**Если winget не установлен:**

<details>
<summary><strong>Способ 1: Через Microsoft Store</strong></summary>

1. Откройте **Microsoft Store**
2. Найдите **"App Installer"**
3. Нажмите "Обновить" или "Получить"
4. Перезапустите PowerShell

</details>

<details>
<summary><strong>Способ 2: Прямая загрузка</strong></summary>

Выполните в PowerShell от имени администратора:

```powershell
$progressPreference = 'silentlyContinue'
Invoke-WebRequest -Uri https://aka.ms/getwinget -OutFile "$env:TEMP\Microsoft.DesktopAppInstaller.msixbundle"
Add-AppxPackage -Path "$env:TEMP\Microsoft.DesktopAppInstaller.msixbundle"
```

Перезапустите PowerShell после установки.

</details>

**Альтернатива:** Если не хотите устанавливать winget, используйте ручную установку согласно [docs/01-setup-environment.md](../docs/01-setup-environment.md).

---

## Быстрый старт

1. **Установите базовые инструменты:**
   ```powershell
   .\windows-setup.ps1
   ```

2. **Проверьте установку:**
   ```powershell
   .\check-installation.ps1
   ```

3. **(Опционально) Установите CLI-инструменты:**
   ```powershell
   .\install-cli-tools.ps1
   ```

4. **При необходимости удалите всё:**
   ```powershell
   .\uninstall.ps1
   ```

---

## Часто задаваемые вопросы

### Скрипт не запускается: "не является внутренней или внешней командой"

**Решение**: Разрешите выполнение скриптов PowerShell:

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка "Требуются права администратора"

**Решение**: Запустите PowerShell от имени администратора:
- `Win + X` → "Windows PowerShell (Администратор)"

### winget не найден

**Решение**: Обновите Windows или установите [App Installer](https://www.microsoft.com/store/productId/9NBLGGH4NNS1) из Microsoft Store.

### Чем отличается windows-setup.ps1 от install-cli-tools.ps1?

**windows-setup.ps1:**
- Устанавливает Desktop-приложения (Claude Desktop, ChatGPT Desktop)
- Работает с подписками (Claude Pro, ChatGPT Plus)
- Не требует API-ключей

**install-cli-tools.ps1:**
- Устанавливает CLI-инструменты (Claude Code CLI, Codex CLI)
- Требует API-доступа и pay-as-you-go оплаты
- Требует API-ключи от Anthropic и OpenAI

### Как полностью удалить всё установленное?

**Вариант 1 (интерактивный):**
```powershell
.\uninstall.ps1
```
Скрипт спросит подтверждение для каждого компонента.

**Вариант 2 (автоматический):**
```powershell
.\uninstall.ps1 -Force
```
Удалит всё без запросов.

**Вариант 3 (выборочный):**
```powershell
# Удалить только CLI, оставить Desktop-приложения
.\uninstall.ps1 -SkipDesktopApps

# Удалить всё кроме VS Code
.\uninstall.ps1 -Force -SkipVSCode
```

### Команды не работают после установки

**Решение**: Перезапустите терминал или обновите PATH:

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

### Как удалить все установленное?

Скрипт деинсталляции не предоставлен, но вы можете удалить инструменты вручную:

```powershell
# Удаление через winget
winget uninstall OpenJS.NodeJS.LTS
winget uninstall Git.Git
winget uninstall Microsoft.VisualStudioCode
winget uninstall GitHub.cli

# Удаление npm пакетов
npm uninstall -g @anthropic-ai/claude-code
npm uninstall -g @openai/codex
npm uninstall -g firebase-tools
npm uninstall -g @anthropic-ai/mcp-server-filesystem
npm uninstall -g @anthropic-ai/mcp-server-github
npm uninstall -g @anthropic-ai/mcp-server-memory
```

---

## После установки

### 1. Настройте API ключи

```powershell
# Anthropic (Claude)
[Environment]::SetEnvironmentVariable("ANTHROPIC_API_KEY", "sk-ant-...", "User")

# OpenAI (Codex)
[Environment]::SetEnvironmentVariable("OPENAI_API_KEY", "sk-...", "User")
```

Получите ключи:
- Anthropic: https://platform.anthropic.com/
- OpenAI: https://platform.openai.com/

### 2. Авторизуйтесь в сервисах

```powershell
# GitHub
gh auth login

# Firebase
firebase login
```

### 3. Настройте MCP серверы

#### Получение GitHub токена

Скрипт спрашивает GitHub токен для настройки MCP сервера. Вот как его получить:

1. Перейдите на https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте имя токену: `Claude MCP Server`
4. Выберите срок действия: `90 days` или `No expiration`
5. Выберите разрешения (scopes):
   - ✅ `repo` (полный доступ к репозиториям)
   - ✅ `read:org` (чтение организации)
   - ✅ `read:user` (чтение профиля)
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (начинается с `ghp_`) — он показывается только один раз!

#### Добавление токена

Скрипт автоматически попросит ввести токен и добавит его в конфигурацию.

Или вручную отредактируйте файл:

```powershell
notepad "$env:APPDATA\claude\settings.json"
```

Добавьте токен в секцию `github.env.GITHUB_TOKEN`:

```json
"github": {
  "env": {
    "GITHUB_TOKEN": "ghp_ваш_токен_здесь"
  }
}
```

**Важно**: Никогда не коммитьте токен в Git!

### 4. Проверьте установку

```powershell
.\check-installation.ps1
```

---

## Дополнительные ресурсы

- [docs/01-setup-environment.md](../docs/01-setup-environment.md) — Подробная инструкция по ручной установке
- [docs/02-github-ai-agents.md](../docs/02-github-ai-agents.md) — Работа с GitHub через AI-агентов
- [docs/03-firebase-integration.md](../docs/03-firebase-integration.md) — Интеграция с Firebase
- [04-advanced-mcp.md](../04-advanced-mcp.md) — Продвинутые MCP серверы
- [05-wsl-setup.md](../05-wsl-setup.md) — Настройка WSL для Windows

---

## Поддержка

При возникновении проблем:

1. Запустите `check-installation.ps1` для диагностики
2. Проверьте разделы Troubleshooting в основных инструкциях
3. Убедитесь, что у вас последняя версия Windows 10/11
4. Проверьте наличие интернет-соединения

---

**Примечание**: Скрипты предназначены для Windows 10/11. Для других ОС используйте ручную установку согласно [docs/01-setup-environment.md](../docs/01-setup-environment.md).
